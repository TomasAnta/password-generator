import { DefaultTheme } from "styled-components";

import { colors, fontSizes, fontWeights, lineHeights, responsiveBreakPoints, spacings } from "./DesignVariables";

export const invitationTheme: DefaultTheme = {
    theme: "invitationTheme",
    colors: colors,
    media: responsiveBreakPoints,
    fontSizes: fontSizes,
    spacings: spacings,
    lineHeights: lineHeights,
    fontWeights: fontWeights,
};
